require_relative 'Manifest'

def parseManifest(data)
    manifest = Manifest.new()

    variable = ""
    tok = ""
    for ch in source.split(//)
        tok += c
        if variable == "sources":
        end
        if tok == "sources"
            variable = tok
            tok = ""
        end
    end
end